name: Builder
on: [push, pull_request]
jobs:
  build:
    name: ${{ matrix.image }}
    container: 
      image: docker://swift:${{ matrix.image }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [xenial, bionic]
    steps:
    - uses: actions/checkout@v1
      with:
        submodules: recursive
    - name: Install WebKit
      run: apt-get -q update && apt-get -y install libwebkitgtk-dev
    - name: Build
      run: swift build -v
